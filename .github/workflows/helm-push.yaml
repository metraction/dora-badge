name: Push Helm Chart

on:
  push:
    branches:
      - main
    tags:
      - 'v*'

jobs:
  push-helm-chart:
    uses: Tiktai/helm-publish/.github/workflows/helm-push.yaml@main
    with:
      chart_dir: helm/dora-badge
      chart_name: dora-badge
      destination: .
      registry: oci://ghcr.io/tiktai/charts
    secrets: inherit
